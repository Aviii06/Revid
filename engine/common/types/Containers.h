#pragma once
#include <vector>
#include <string>
#include <map>
#include <set>

template <class T>
using Vector = std::vector<T>;

using String = std::string;

template <class T, class U>
using Map = std::map<T, U>;

template <class T>
using Set = std::set<T>;
